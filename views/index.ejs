<html>
    <head>
        <title>PIPS-USGS</title>
        <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
        <meta property="og:url" content="https://pips-usgs/"/>
        <meta property="og:title" content="Planetary Image Processing Server"/>
        <meta property="og:type" content="application"/>
        <meta property="og:description" content="A publication tool for The USGS, and other agencies, researchers"/>

        <link rel="stylesheet" type="text/css" href="css/pips.css">

        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css"
        integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
        
        <script type="text/javascript" src="http://ajax.googleapis.com/ajax/libs/jquery/1.6.2/jquery.min.js"></script>
    </head>

    <body>  
        <img src="../images/usgsLogo.png" class="float-md-left" width="10%" height="auto" style="margin: 0%; padding: 0%"><br>
        <div id="loading" class="float-md-right lead" style="visibility:hidden">
                <img src="../images/loading.gif" class='float-md-left;' height="125px" weight="135px"/><br>Loading
        </div>
        <br/>
       
            <div class="text-center" style="border: 0px solid black; padding:25%; padding-top: 0; padding-bottom: 0;">
                <h1 class="display-3" style="border: 0px solid blue;">Planetary Image Publication Server</h1> 
                <h4 class="display-5">Powered by ISIS3</h4>
            </div>
           
     
        <br/>
        <br/>
        <hr class="my-9">
        <div class="title text-center" style="width: auto;">
            <p class="lead" style="margin-left: auto; margin-right: auto; font-size: 2rem;">You can upload .cub file and .tpl file </p>
                <a class="btn btn-secondary btn-lg" style= "border-color: black; color: white;" role="button" onclick="alert('1. Upload a .cub or a .tif file\n\n2. Input the desired output size of the figure\nor leave blank for Raw Image size')">Help</a>
                <a class="btn btn-secondary btn-lg" href= '/tpl'
                    role="button">What is a TPL file?</a>
        </div>
        <br/>
        <div class="container">
            <form class="text-center" action="/upload" method="POST" enctype="multipart/form-data">
                <div class="btn btn-primary btn-lg form-group" style=" width: 25rem;display: inline-block;">
                    <label for="cubUpload" style="font-size: x-large;">Choose an ISIS Cube File</label>
                    <input name="uploadFile" id="cubUpload" style="padding-bottom:1rem;"type="file" class="form-control-file" />

                    <div class="form-group float-center" style="padding-left: 7rem; padding-right:0;padding-top: -1rem; width: 17rem;">
                            <input class="form-control form-control-sm text-center" type="text" id="widthInput" name="desiredWidth" placeholder="Output Width (Px)"/>
                            <input class="form-control form-control-sm text-center" type="text" id="heightInput" name="desiredHeight" placeholder="Output Height (Px)"/>
                    </div>
                </div>

                
                <br/>
                <hr class="style5">
                <div class="btn btn-primary btn-lg form-group">
                    <label for="tplUpload" style="font-size: larger">Choose a Template File</label>
                    <input name="templateFile" id="tplUpload" type="file" class="form-control-file" />
                </div>
                <br/>
                <input type="submit" class="btn btn-secondary btn-lg" name="SubmitCube" value="Upload" 
                    style="width: 10rem;" onclick="loaderActivate()" />
            </form>
        </div>

        <div id="info-alert" style= "visibility:hidden" class = "alert alert-info text-center alertBoxes">
            <button id="infoBtn"class = "close" aria-hidden = "true" onclick="setNotVisible(this)">
                &times;
            </button>
            Upload a .cub file
        </div>
        
        <div id="fail-alert" style= "visibility:hidden" class = "alert alert-primary text-center">
            <button id="failBtn"class = "close" aria-hidden = "true" onclick="setNotVisible(this)">
                &times;
            </button>
            Must start with upload
        </div>
        <div id="fail-image" style= "visibility:hidden" class = "alert alert-primary text-center">
            <button id="failImageBtn"class = "close" aria-hidden = "true" onclick="setNotVisible(this)">
                &times;
            </button>
            Failed to create image from upload
        </div>
        <div id="fail-upload" style= "visibility:hidden" class = "alert alert-primary text-center">
            <button id="failUploadBtn"class = "close" aria-hidden = "true" onclick="setNotVisible(this)">
                &times;
            </button>
            Failed to Upload
        </div>
        <div id="tplAlert" style= 'visibility:hidden' class = "alert alert-warning text-center" role="alert">
            <button id="tplBtn" class = "close" data-dismiss = "alert" aria-hidden = "true" onclick="setNotVisible(this)">
            &times;
            </button>
                    
            Template must be (.tpl) or left blank.
        </div>

        <div id="server-timeout" style= 'visibility:hidden' class = "alert alert-dark text-center" role="alert">
                <button id="timeBtn" class = "close" data-dismiss = "alert" aria-hidden = "true" onclick="setNotVisible(this)">
                &times;
                </button>
                        
                Server Timed Out please Reupload a cube
            </div>

        <script type="text/javascript">
            // get needed DOM element
            var loader = document.getElementById('loading');

            // get the input doms for dimensions
            var widthInputBox = document.getElementById("widthInput");
            var heightInputBox = document.getElementById("heightInput");

            // set alert flag for warning the user of input size
            var alerted = false;

            // get the ids of the different alert boxes
            var InfoBox = 'info-alert',
                tplBox = 'tplAlert',
                failureBox = 'fail-alert',
                uploadFail = 'fail-upload',
                imageFail = 'fail-image',
                timeOut = "server-timeout";

            // parse the url and flag the appropriate error messages
            function fixLoader(){
                if(<%= alertCode %> == 1){
                    var alert = document.getElementById(InfoBox);
                    alert.style.visibility = 'visible';
                }
                else if(<%= alertCode %> == 2){
                    var alert = document.getElementById(tplBox);
                    alert.style.visibility = 'visible';
                }
                else if(<%= alertCode %> == 4){
                    var alert = document.getElementById(failureBox);
                    alert.style.visibility = 'visible';
                }
                else if(<%= alertCode %> == 5){
                    var alert = document.getElementById(uploadFail);
                    alert.style.visibility = 'visible';
                }
                else if(<%= alertCode %> == 6){
                    var alert = document.getElementById(imageFail);
                    alert.style.visibility = 'visible';
                }
                else if(<%= alertCode %> == 7){
                    var alert = document.getElementById(timeOut);
                    alert.style.visibility = 'visible';
                }
                else if(<%= alertCode %> != 0){
                    var alert = document.getElementById(InfoBox);
                    alert.style.visibility = 'visible';
                    var alert2 = document.getElementById(tplBox);
                    alert2.style.visibility = 'visible';

                }
            }
        
            /* get the id of the parent element of the button that was clicked and
             hide the div box this is to help make the x buttons on the alerts work properly */
            function setNotVisible(element){
                var hideElement = document.getElementById($(element).parent().attr('id'));
                hideElement.style.visibility = 'hidden';    
            }

            // showing an hiding the loading gif
            function loadInvisible(){
                loader.style.visibility = 'hidden';    
            }
            function loaderActivate(){
                loader.style.visibility = 'visible';
            }

            
            // contantly chekc the input boxes just in case they are too big to render on the users screen
            function checkInput(){
                let width = widthInputBox.value;
                let height = heightInputBox.value;

                // alert of the are too large
                // the flag is used to only alert the user 1 time
                if(!alerted){
                    if(width >= window.screen.width-window.screen.width*.2){
                        alert("Warning: The selected width is too larger \n     Continuing will cause redering issues");
                        alerted = true;
                    }
                    else if(height >= window.screen.height-window.screen.height*.2){
                        alert("Warning: The selected height is too larger \n    Continuing will cause redering issues");
                        alerted = true;
                    }
                }
            }
            // interval for the function above =  1.5 seconds
            setInterval(checkInput,1500);


            // this bind call is used to make the loader gif invisible on the page load
            $(window).bind("pageshow", function(event) {
                if (event.originalEvent.persisted) {
                    // functions to run when page loads
                    loadInvisible();
                    fixLoader(); 
                }else{
                    fixLoader();
                }   
            });
            
        </script>
        
    </body>
</html>
